# Daily review — 2026-09-26

**Nothing needs a decision that costs money today** — nothing traded, the tag table has been frozen
~145h, no hypothesis is near a threshold. One new finding is worth knowing before any restart:
`book_staleness` reports `ok` *exactly* when the loop stalls worst, so a green STATE.md is not
evidence the book feed is healthy.

## 1. Material change since 2026-09-25

**Third stale-book episode, ~9 hours, self-cleared again.**

| time (UTC) | median book age | state |
|---|---|---|
| 09-25 19:39 | 2,482 ms | FIRING |
| 09-25 20:39 | 29,631 ms | FIRING |
| … 10 publishes in the 29,614–31,536 ms band … | | |
| 09-26 05:41 | 30,101 ms | FIRING |
| 09-26 06:41 | 3,512 ms | FIRING |
| 09-26 07:41 | 1,632 ms | ok |

Since 07:41 it has run 1,632–2,500 ms across six publishes, still straddling the 2,000 ms line
(currently FIRING at 2,500 ms). This is the third degrade-and-self-clear cycle in reach (09-22
06:24, 09-24 21:35, 09-26 06:41), with no trigger visible from this checkout in either direction.
The episode ran ~9h against the prior ~48h; at n=3 that is not a trend and I am not calling it one.

**Loop cadence: worst cycle in the visible history, then full recovery.** The six publishes 09-25
12:37 → 17:38 report max cycles of 3,718–3,946s — a 66-minute cycle, beating the 3,718s record
flagged yesterday. Then 54–507s, then 57–109s through the stale episode, then **32–36s from 06:41
onward**, matching the 09-22 healthy reference. Cadence recovered in the same publish staleness
cleared — the third time those two have moved together.

**New: the staleness monitor goes blind exactly when cadence is worst.** Across all 48 publishes in
reach, the split is clean with no overlap either way:

| publishes | reported max cycle | `book_staleness` line |
|---|---|---|
| 9 | 2,946–3,946s | `no signals in last hour` → **`ok`** |
| 39 | 33–1,450s | a measured value |

A cycle longer than the monitor's 1-hour lookback emits no book signal, the monitor has no data, and
no-data collapses into `ok`. I cannot confirm that mechanism — `ops/publish.py` is not in this
checkout. One alternative is visible in the code that is: `engine/kalshi_bias_engine/runtime/
settings.py:67-75` persists a signal only when oracle staleness is FRESH *and* (edge gap ≥ $0.02 or
top-20 rank), dropping the rest at a 2% sample rate. If the monitor reads persisted signals, then
both "no signals" and the median itself are computed on a filtered subset. The cadence correlation
favours the stall explanation — a write filter has no reason to track cycle length.

Either way this changes the standing of a flag the last four reviews carried as cosmetic masking:
**the all-clear is not independent of the fault it would report.** *Falsifiable:* the next publish
reporting a max cycle above ~3,600s should again read `no signals`/`ok`; a masked publish at a max
cycle under ~2,000s falsifies the link.

**Third `$0.00` equity print** — 09-25 16:38 (`-44.08 over 24h`). Three now in ~33h (09-24 07:32,
09-25 10:36, 09-25 16:38), two of them within six hours. Still a halted account with no fill in
~145h, which cannot go to zero and back inside the hour.

**The 24h delta field carries the same zero fault, and does not reconcile as cleanly as the last two
reviews reported.** Checking all 25 publishes that have a 24h-earlier counterpart in reach against
"the published row at or before T-24h": 16 match to the cent, 9 do not. Two are structural, and they
contradict each other:

- **09-26 06:41 reports `+34.02 over 24h`** on equity $34.02 — exactly a $0.00 baseline. No zero was
  published near 09-25 06:3x; that hour reads $35.33, and the *next* publish (07:41) reports -1.31
  against exactly that. To anything reading the field, that publish showed a **+100% day on a halted
  account.**
- **09-26 10:41 reports -1.31**, i.e. a $35.33 baseline — although the published row at or before
  T-24h *is* the 09-25 10:36 zero.

So a zero baseline was used where none was published, and skipped where one was. The other 7
mismatches are $0.20–$1.39 and are consistent with a baseline store finer-grained than the hourly
publish I can see, so I am not calling those faults. The consequence is the part that matters: the
delta field cannot be used to validate the equity field, which is how the 09-24 and 09-25 reviews
reasoned about the zeros. Neither is a second independent source for the other.

**Yesterday's falsifiable test failed — dropping the claim.** 09-25 predicted that if the equity mark
is priced off the same book feed, $1–6 hourly swings would resume once staleness returned to ~30s.
Staleness returned to ~30s for ten consecutive publishes (09-25 20:39 → 09-26 05:41). Equity across
that window was $33.89 at eight of them and then $34.02 — a single $0.13 move, nothing near the
$0.50 threshold the test set. Per its own criterion the mark↔book-feed link is wrong and is retired.
What is left is a mark **near-frozen for ~34h across four values** ($35.33 → $34.15 → $33.89 →
$34.02, range $1.44) against $39.96–$45.40 over the preceding 48h. Equally consistent with a mark
that updates rarely and one that has stopped; nothing here distinguishes them, and the book-feed
candidate is now gone.

**Static.** HALTED throughout, same pre-registered reason. The tag table is byte-identical to 09-25
12:37 and to every publish in reach (109/36/+$2.05; 470/216/-$29.01; pooled 579/-$26.96/-$0.0466),
arithmetic still internally consistent: 109+470=579, 2.05-29.01=-26.96, -29.01/470=-0.0617,
-26.96/579=-0.0466. Prior reviews date the freeze to 09-20 11:12, ~145h. `Active slice gates` 0
throughout. `order_rejection` 0/0. `pnl_breach` moved 6 → 5 settlement-days at 09-25 16:38 — the
14-day window rolling, not an event. No settlement progress: 252 settled / 327 outstanding,
unchanged for the whole history in reach.

**Reach limit:** git history here is shallow and grafted at 09-24 12:33, so "all 48 publishes" means
09-24 12:33 → 09-26 12:42 (the 09-24 14:33 publish is absent). The larger counts in the 09-23 and
09-24 reviews came from a deeper window I can no longer see.

## 2. Hypotheses approaching sample size or kill condition

None. H-001, H-002 and H-003 are all REFUTED; nothing is in `EVALUATING`; REGISTRY.md is untouched.
No tag has taken a fill in ~145h, so there is no live test that could be called early and nothing
near a threshold.

Not proposing a re-run of anything refuted, and not proposing a search for a better filter over data
already used. The two open cross-strike bracket identities remain unregistered, and their stated
precondition — measuring how long violations persist — still needs a real book tape this checkout
does not have.

## 3. Questions for a human

1. **New, and the one I would act on: does `book_staleness` reading `ok` ever mean "measured and
   fine"?** In everything visible here, `ok` on that monitor is either a value under 2,000 ms or no
   data at all — and the no-data case happens precisely during the worst loop stalls. Before any
   restart, a green STATE.md should not be read as a healthy feed.
2. **Carried, downgraded: what degrades the book feed to ~30s, and what clears it?** Three cycles
   now, no visible trigger either way. 30-second books are what H-001's entry rules exist to exclude.
3. **Carried, raised: does any halt, gate or alert key on equity or the 24h delta?** The delta
   printed a +100% day on a halted account this morning. Harmless while halted; not before anything
   trades.
4. **Carried: is the mark stuck or stable, and what prices it?** Near-frozen ~34h. The book-feed
   answer is retired and there is no candidate to replace it.
5. **Carried, low priority: what turned `Active slice gates` to 0 at 09-24 06:32?** Still 0, still no
   theory attached.

No engine change is proposed and none is assumed. I am read-only with respect to the box; detection
and the pre-registered kill rule are already automated there, and I cannot halt or restart anything.
